/**
 * @deprecated Replaced by Pinia stores (useOrdersStore, useDealersStore, etc.)
 * Kept as an empty shim for any straggling imports. Will be removed.
 */
export const useAdminDashboard = () => ({})
